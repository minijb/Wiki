# LLM Wiki 工作区初始化计划

## Context

根据 Karpathy 的 LLM Wiki 理念（[gist](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)），在当前目录创建三层架构的 Wiki 工作区。核心目标：建立一个 LLM 可持续维护的持久化知识库骨架。

Karpathy 核心比喻：**Obsidian 是 IDE，LLM 是程序员，Wiki 是代码库**。

## 目录结构

```
F:\Project_AI\wiki/
├── CLAUDE.md                     # Schema 层 — Wiki 结构约定与角色（根目录）
├── .claude/
│   └── skills/
│       ├── ingest.md             # /ingest 技能 — 摄取新来源
│       ├── query.md              # /query  技能 — 查询 Wiki
│       ├── lint.md               # /lint   技能 — 健康检查 Wiki
│       └── refine.md             # /refine 技能 — 讨论打磨笔记
├── raw/                          # 原始来源层 — 不可变，LLM 只读不写
│   ├── assets/                   # 本地图片等附件
│   ├── gamedev/                  # 游戏领域知识
│   │   ├── rendering/            # 渲染、Shader、URP/HDRP
│   │   ├── physics/              # 物理、碰撞检测
│   │   ├── animation/            # 动画、程序化动画
│   │   ├── networking/           # 网络同步、多人游戏
│   │   ├── ai/                   # AI、行为树、NavMesh
│   │   ├── gameplay/             # 玩法系统、战斗、技能
│   │   ├── ui/                   # UGUI、UI Toolkit
│   │   ├── audio/                # 音频系统
│   │   └── optimization/         # 性能优化、内存管理
│   ├── cs/                       # 计算机科学基础
│   │   ├── languages/            # C#, C++, Lua, Python
│   │   ├── design-patterns/      # GoF, 游戏编程模式
│   │   ├── architecture/         # SOLID, DDD, ECS, 软件架构
│   │   └── algorithms/           # 算法与数据结构
│   └── tools/                    # 工具与工作流
│       ├── git/                  # 版本控制
│       ├── ide/                  # Rider, VS, VS Code
│       ├── docker/               # 容器化
│       └── ci-cd/                # 持续集成/部署
├── drafts/                       # 用户不完善笔记 — LLM 讨论打磨区（非 Karpathy 原生，但实用）
├── wiki/                         # Wiki 层 — LLM 生成和拥有的内容
│   ├── index.md                  # 内容导向目录 — 所有 Wiki 页面的索引
│   ├── log.md                    # 时间导向日志 — 操作记录，只追加
│   ├── concepts/                 # 概念页面 — 抽象概念/理论/方法论
│   ├── entities/                 # 实体页面 — 具体事物/人物/项目
│   ├── sources/                  # 来源摘要页面 — 每篇 raw 来源对应一个摘要
│   ├── comparisons/              # 对比分析页面 — 跨来源的矛盾/对比
│   └── overview.md               # 高层综合页面 — 所有已摄入内容的整合
└── .qmd/                         # QMD 索引缓存（自动生成，不手动编辑）
```

## 计划步骤

### 1. 创建目录结构
- `.claude/skills/` — 技能定义目录
- `raw/` — 原始来源（不可变层），三大支柱：`gamedev/`（9个子领域）、`cs/`（4个子领域）、`tools/`（4个子领域）+ `assets/`
- `drafts/` — 用户不完善笔记的独立存放目录
- `wiki/` — Wiki 页面目录（LLM 拥有层），含子目录 `concepts/`、`entities/`、`sources/`、`comparisons/`
- `.qmd/` — QMD 索引缓存目录（由 QMD 自动管理）

### 2. 创建四个 Skill 文件（核心工作流）

每个 Skill 用 markdown + YAML frontmatter 定义，可被 `/skill-name` 调用：

| 技能 | 命令 | 职责 |
|------|------|------|
| `ingest.md` | `/ingest` | 阅读 raw/ 来源 → 与人类讨论要点 → 写摘要页面到 wiki/sources/ → 更新 wiki/concepts/ 和 wiki/entities/ → 更新 wiki/index.md → 追加 wiki/log.md |
| `query.md` | `/query` | 先读取 wiki/index.md 定位，再调用 QMD（BM25+向量搜索+LLM重排序）精准检索 → 阅读并综合 → 生成答案（支持 markdown/表格/幻灯片/图表） → 好答案归档回 wiki/ |
| `lint.md` | `/lint` | QMD 全局搜索检测孤立页面与断链 → 检查矛盾声明 → 陈旧内容 → 缺失交叉引用 → 数据缺口 → 建议新来源/新问题 |
| `refine.md` | `/refine` | 读取 drafts/ 中不完善笔记 → 指出遗漏、模糊、矛盾之处 → 与用户讨论 → 根据讨论优化笔记 → 完善后标记可归档 |

### 3. 创建 CLAUDE.md（Schema 层）
在项目根目录创建 `CLAUDE.md`，定义 Wiki 的静态约定：
- Wiki 的目录结构约定和命名规范（wiki/ 子目录分类）
- 页面格式规范（frontmatter 字段、标题层级、交叉引用语法 `[[wikilink]]`）
- 页面类型定义：concepts、entities、sources（来源摘要）、comparisons
- wiki/index.md 和 wiki/log.md 的格式规范
- 人类与 LLM 的分工原则（人类策展来源/提问/思考 — LLM 写wiki/维护索引/检查）
- 指向四个 Skill 作为标准操作入口

### 4. 创建 wiki/index.md（内容导向目录）
在 `wiki/` 内创建，作为 Wiki 所有页面的内容导向索引：
- 按页面类型分类（Concepts、Entities、Source Summaries、Comparisons、Overview）
- 每项：`[[页面路径|标题]] — 一行摘要 [可选元数据]`
- 标记为"待首次 Ingest 后填充"

### 5. 创建 wiki/log.md（时间导向日志）
在 `wiki/` 内创建，作为操作日志：
- 只追加，不修改已有条目
- 条目格式：`## [YYYY-MM-DD] 操作类型 | 摘要`
- 使用 Karpathy 推荐的 `grep "^## \[" log.md | tail -5` 可解析格式
- 初始条目：工作区创建记录

### 6. 配置 QMD 搜索引擎
- 确认 `qmd:qmd` skill 可用（MCP 服务器已连接）
- 创建 `.qmd/` 索引缓存目录
- 在首次 `/ingest` 后触发 QMD 建立初始索引
- 后续每次 `/ingest` 操作后自动更新 QMD 索引

### 7. 将 学习笔记.md 归入 raw/
将现有概念文档移入 `raw/`，作为首个来源。

## 关键设计决策

- **三层架构（Karpathy 核心）**：`raw/`（不可变来源）→ `wiki/`（LLM 拥有）→ `CLAUDE.md`（Schema 约定）
- **Skills 承载工作流，CLAUDE.md 承载约定**：四种操作是"行为"，用可调用的 Skill 定义；目录结构、页面格式是"数据"，由根目录 CLAUDE.md 描述
- **raw/ 三大支柱**：`gamedev/`（游戏实践知识）、`cs/`（计算机科学理论）、`tools/`（工具操作流程）。三者知识类型不同，分开定位
- **wiki/ 按页面类型分子目录**：`concepts/`、`entities/`、`sources/`（来源摘要）、`comparisons/`。匹配 Karpathy 的页面类型分类
- **wiki/index.md + wiki/log.md 在 wiki/ 内部**：Karpathy 规范中 index 和 log 是 Wiki 的一部分，不是外部文件
- **drafts/ 是扩展**：非 Karpathy 原生设计，但作为 `/refine` 的打磨区很有实用价值
- **wiki/ 完全由 LLM 拥有**：人类阅读，LLM 写作和维护
- **QMD 作为搜索引擎**：本地 BM25 + 向量搜索 + LLM 重排序，`.qmd/` 由 QMD 自动管理

## 验证

1. 确认目录结构创建完整（含 `raw/assets/`、`wiki/concepts/`、`wiki/entities/`、`wiki/sources/`、`wiki/comparisons/`、`.qmd/`）
2. 确认四个 Skill 文件存在且包含完整工作流步骤（`/query` 和 `/lint` 已集成 QMD 调用）
3. 确认根目录 CLAUDE.md 描述 Wiki 约定并引用四个 Skill
4. 确认 QMD MCP 服务器可正常调用，`qmd:qmd` skill 就绪
5. 确认 wiki/index.md 和 wiki/log.md 有初始内容
6. 用 `/ingest` 对 `学习笔记.md` 执行首次摄取，验证全流程（含 QMD 索引更新）
