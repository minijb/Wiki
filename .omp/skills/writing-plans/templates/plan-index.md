# Active Plans Index

> 全局计划索引 — 记录所有活跃计划及其依赖关系。
> 每次创建新计划前先查询此文件，避免重复和冲突。
> 计划按主题（domain）分组，创建时通过 `--domain` 指定。

---

## 📁 未分类

> 未指定主题的计划（默认），或旧格式迁移至此。

### 任务组（有先后依赖的计划链）

> 计划之间存在顺序依赖关系，排列顺序即为执行顺序。

| 序号 | 计划名 | 类型 | 摘要 | 依赖 | 状态 |
|------|--------|------|------|------|------|
| — | — | — | — | — | — |

<!-- 依赖列填写依赖的计划名，多个用逗号分隔。无依赖填 `—` -->

### 独立任务（无依赖，可并行）

> 这些计划与其他计划无依赖关系，可按任意顺序执行。

| 计划名 | 类型 | 摘要 | 状态 |
|--------|------|------|------|
| — | — | — | — |

---

> **格式说明：**
> - **主题**：`## 📁 主题名` — 创建计划时通过 `--domain` 指定，新主题自动创建节
> - **序号**：任务组中的执行顺序（1, 2, 3...），按拓扑排序
> - **类型**：`FULL` 或 `QUICK`
> - **摘要**：一句话描述计划目标，方便快速浏览
> - **依赖**：此计划依赖哪些其他计划先完成（同一 domain 内）
> - **状态**：`TODO` / `IN_PROGRESS` / `BLOCKED` / `DONE`
>
> Domain 节按字母序排列，"未分类"始终在最后。
> 新主题出现时，按字母序插入到正确位置。
> 计划归档时，对应条目自动迁移到 `PLAN_COMPLETED.md`。
